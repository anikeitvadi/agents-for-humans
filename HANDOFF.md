# Handoff (updated 2026-09-11 AM)

**Session 2026-09-11 AM (Claude, on Ani's machine):** pulled merged `main` (PR #1), created `.venv` locally (uv, Python 3.13), `pytest -q` → 109 passed, 4 deselected. Ran the real app (`uvicorn agent.app:app`) and hit every endpoint on a clean `data/demo.db`: sample case surfaces with draft, repeat is silent with the same draft; bulletin Sep silent / Oct surfaced with draft; recall `mode: live` against CPSC, surfaced then silent on repeat, unmatched receipt silent. Partner (DarshanHari19) has **accepted** the collaborator invite (write). Repo is still **private**. No AWS CLI, credentials, or `agentcore` CLI on this machine. Changes this session: README reconciled with the real code (module map, demo flows, scope limits, setup; stale `fields.json` and "50 tests" removed) and `docs/architecture.svg` added (built vs. production path), embedded in the README.

**Next steps, in order (as of 2026-09-11 AM):**
1. **AWS credits: claim by TODAY Fri Sep 11, 12pm PT, per person** (rule in `docs/brief.md`). Both builders.
2. **Flip the repo public** and confirm "MIT License" shows in the GitHub About section. Required for submission; everything in the repo is already clean for it.
3. **Decide ownership split and repo name** (placeholder "Clockwork" vs. current "Immigration Status Guardian"). Blocks splitting the remaining work.
4. **AgentCore Runtime deploy attempt** (Day-3, time-boxed): needs AWS credentials + `bedrock-agentcore` on a machine; `deploy/agentcore_entrypoint.py` is unverified. Also run `pytest -m live` once credentials exist (live Bedrock extraction + CPSC).
5. **Manual phone/browser click-through** of the UI by a human: `uvicorn agent.app:app --reload` then http://127.0.0.1:8000/ (wipe `data/demo.db` first).
6. **Record the 5-minute video** (problem / who / why; use `docs/architecture.svg` for the spine) and write the three builder.aws posts.


**UI redesign done (2026-09-10)** — full rewrite of `ui/index.html` per a detailed design brief (decision-first hierarchy, two-tab layout, translated internal-state language, refined dark palette, Motion + Lucide for three deliberate animation moments). Highlights:
- **Hierarchy flip:** the document-date comparison (headline, side-by-side dates, connector showing the day gap, "Review attorney draft" CTA) is now the first thing shown, above all parsing/process detail. Demo-trigger buttons moved into a collapsible `<details class="demo-controls">`, de-emphasized as what they are — a way to simulate uploads/scheduled checks without a live upload.
- **Two tabs:** "Case review" (immigration) and "Recall demo," each with its own review card, supporting documents/activity, and demo controls — replacing one long single-column page.
- **Language translated to outcomes:** `failed: novelty` → "Already flagged · Draft available"; `surfaced`/`silent` → "Needs review"/"No new notification"; raw reason strings moved into a collapsed "Technical detail" `<details>` per activity row. The "one thing needs you" badge is now a dynamic status chip reflecting actual state, never a fixed label.
- **Palette:** exact hex tokens from the brief (`#10151B` bg / `#171E27` surface / `#202A35` raised / `#E7EDF3` ink / `#58BFA9` teal / `#D8B16A` amber); dropped the second display font and the ALL-CAPS eyebrow per the frontend-design skill's guidance on avoiding generic AI-tell chrome; legal notice is now a compact left-bordered note, not a banner.
- **Motion (motion.dev, pinned `@13.2.0`) + Lucide (`@1.44.0`)** via CDN for three moments only: clicking a document highlights its date in the comparison; the comparison's day-gap connector draws in and fades in the gap label on load; the attorney draft opens as a slide-in side panel (preserves scroll position by construction). All respect `prefers-reduced-motion` by applying the end state instantly instead of animating.
- **Resilience:** core interactivity (all fetch calls, DOM rendering, button wiring, tab switching) lives in a plain classic `<script>` with zero dependency on either CDN resolving — Lucide icons and Motion animations are optional enhancements with graceful instant-state fallbacks (`icon()`/`fx()`), so a slow or blocked CDN degrades polish, never function. This was deliberate: earlier in the session a *different* bug (a stale zombie server process on an old port) looked exactly like "buttons don't work," and a first draft of this redesign's Motion loading would have reintroduced a similar-looking failure mode (blocking on a network import before any button's click listener attached) — fixed before it shipped.
- Real bugs caught and fixed during browser verification (not present in the shipped version): the empty-state hint text stayed visible after the case loaded (`.hidden` class had no matching CSS rule for several elements — now one generic `.hidden { display: none; }` rule); a couple of manually-constructed activity-row objects were missing their `icon` field, rendering no icon (now defaults by tone as a fallback, plus fixed at each call site).
- `tests/test_ui_html_safety.py` rewritten to scan all inline `<script>` blocks regardless of attributes (needed once a `type="module"` script existed) and assert zero `.innerHTML` usage anywhere in this app's own code, comment-aware.
- Verified: `.venv/bin/python -m pytest -q` → 109 passed (unaffected, UI-only change). Browser-verified via live click-throughs and direct DOM dispatch (this environment's tab intermittently isn't OS-foreground, which silently drops real automated clicks without erroring — confirmed harmless to the app itself by cross-checking with direct `dispatchEvent`/function calls): sample case load + repeat (draft retention), side panel open/close, document-highlight, tab switching + per-tab header label, gate illustration, both bulletin polls accumulating into one activity feed, recall demo with live CPSC data. Mobile layout verified via DOM source order + CSSOM media-query inspection rather than a true narrow-viewport screenshot (the resize tool didn't produce a real viewport-width change in this environment).
- Not yet done: no user has looked at this on a real phone; the "205 days"-style technical detail text under each activity row is still fairly raw (shows the internal `reason` string, e.g. `failed: materiality, actionability, window`) — acceptable per the brief ("keep technical explanations in an expandable details area") but could be translated further in a future pass.

**Next steps as of 2026-09-10 PM (superseded by the 2026-09-11 list above):** PR #1 merged 2026-09-11; partner invite accepted; README reconciled and diagram exported 2026-09-11. Still open: flip public, ownership/rename, AgentCore deploy, video + posts.

**Browser verification done (2026-09-10), all five demo controls, on a clean `data/demo.db`:**
- Load sample case (twice): correct dates/evidence, 55-day gap flagged, draft renders as plain text (confirms the F5 HTML-injection fix holds visually, not just in the regression test); second click correctly shows "no new ping" while still displaying the same saved draft (C2 confirmed live).
- Show decision gate: one green "surfaced" pill, two gray "silent" pills.
- Run recall demo: labeled "live CPSC feed," real match against the real Cambridge Audio speaker recall ($218/$350); unmatched receipt produces no ping.
- Poll bulletin September 2025: silent/`not_current`, no ping. Poll bulletin October 2025: surfaces "Sample Bulletin Case — EB2-India — current" with a real attorney-review draft, unattended.
- One false alarm caught and resolved: the very first click showed no draft — turned out to be a stale row in `data/demo.db` left over from a `curl` smoke test run during C1, before the draft-persistence feature (C2) existed. Not a real bug; fixed by deleting that file (gitignored, not committed) and restarting clean. Worth remembering: don't reuse `data/demo.db` across sessions spanning a schema/behavior change — wipe it first.
- Everything else from the "browser-based UI verification is still outstanding" note below is now resolved.

**C1–C4 all done (2026-09-10).** C4 (recall through the shared engine) closes out the implementation-handoff's punch list:

**C4 — recall through the shared engine, with a real live CPSC feed:**
- `agent/packs/recalls/feed.py` (new): `fetch_live_recall()` calls the real, no-auth `saferproducts.gov` CPSC API (verified reachable from this environment, unlike travel.state.gov) and normalizes the first recall matching a given manufacturer; returns `None` on any network/HTTP/parse/shape failure rather than raising. `get_recall_item()` tries live first, falls back to `fixtures/recalls/captured_cpsc_recall.json` — a real snapshot (RecallID 10948, "Cambridge Audio Yoyo (M) Portable Bluetooth Speakers," $218 refund / $350 voucher) captured live from the same API on 2026-09-10, not an invented recall. Every result carries `mode: "live"|"fallback"`.
- Replaces the old fictional fixture (`fixtures/sample_case/recall_demo.json` had an invented "Melomania Touch Earbuds" recall that didn't match any real CPSC entry) with `fixtures/recalls/receipts.json`, whose seeded receipt now matches the real captured recall's actual product name.
- `agent/packs/recalls/pipeline.py` (new): `run_recall_check()` runs the match through the same shared `process_event`/gate/`save_draft`/`get_draft` path as immigration (F3's dedup, C2's draft retention) — `agent/app.py::recalls_check` no longer calls `match_recall_to_receipt` directly or bypasses the gate, as the review flagged. Draft here is the deterministic remedy message itself (no LLM call — the recall pack was never meant to draft an email, per architecture-spec.md's "one ping: refund or voucher?").
- `agent/app.py`: the recall item is resolved once at startup (`attempt_live_recall_feed=True` for the real running app; `create_app()`'s default `False` keeps the test suite offline/deterministic — same pattern as C1's extraction mode). `/api/recalls/check` now returns `mode`, `alert`, and `draft` alongside the existing `matched`/`message`. UI recall card labels live vs. fallback.
- New live-marked test (`tests/test_live_recall_feed.py`) — actually run this session (network reachable here) and confirmed the real recall is still live: product name contains "Yoyo", refund option present.
- Verified: `.venv/bin/python -m pytest -q` → 109 passed, 4 deselected (live — 3 were previously deselected without running; this session's live recall test was run explicitly and passed). Smoke-tested via `uvicorn` (real `attempt_live_recall_feed=True` app) + `curl`: seeded receipt → matched, live mode, real $218/$350 remedy, surfaced alert + draft; unmatched receipt → no ping; repeat seeded → silent + same draft (dedup). Not browser-verified (Chrome extension still not connected).
- **All C1–C4 items from `docs/implementation-handoff-2026-09-10.md` are now implemented and test-covered.** Remaining before submission: reconcile README/architecture-spec prose with the now-real behavior (item 4 of that doc's finish checklist — README already didn't reference the old fixtures, but hasn't been read end-to-end against current behavior this session); attempt AgentCore deploy (item 5, needs real AWS/AgentCore CLI access, not available in this sandbox); flip repo public + confirm MIT LICENSE is at the root (LICENSE file exists, repo still private per earlier entries); export the architecture diagram as an image; partner (DarshanHari19) collaborator-invite acceptance still pending; record the demo video. Browser-based UI verification is still outstanding across all of C1–C4 (Chrome extension not connected in this environment all session) — recommend a manual click-through before the demo.

**C3 done (2026-09-10) — unattended bulletin poll, wired with real captured data:**
- `fixtures/bulletins/2025-09.json` + `2025-10.json`: two real, consecutive DOS Visa Bulletin months (EB2-India) paired with the matching-month USCIS chart-selection designation, replacing the placeholder `sample_month.json`. Chosen because they capture a real, well-documented event: USCIS switched the designated employment-based filing chart from Final Action Dates (September) to Dates for Filing (October) — the first bulletin of FY2026 — exactly the two-charts-per-month scenario `check_bulletin_cutoff` exists to handle.
  - **Provenance caveat:** travel.state.gov returned HTTP 403 to automated fetching this session (same class of blocking noted for USDA FSIS in architecture-spec.md §9) — cutoff values are corroborated against independent legal-industry coverage of the same bulletins (AILA, Morgan Lewis, BAL, Fragomen; URLs in each fixture's `verification_sources`) rather than a direct primary-source capture. Flagged in `fixtures/bulletins/README.md`; replace with a direct capture if the block clears.
  - `fixtures/bulletins/seeded_case.json`: synthetic EB2-India case (priority date 2013-06-15) chosen so September is `not_current` and October is `current` — the "historical replay" demo beat.
- `agent/packs/immigration/bulletin.py` (new): `load_bulletin_month()`/`load_seeded_case()` (raise `BulletinLoadError` — never crash — on a missing/malformed source) and `run_bulletin_poll()`, which runs `check_bulletin_cutoff` then the same shared engine/gate/persistence/draft path as the discrepancy pack (F3's dedup, C2's draft retention). Retrogression is only checked when the previous month's chart is the *same type* as the current one — September/October differ (Final Action vs Dates for Filing), so comparing them directly would be a category error, not a real backward move; the module skips it in that case. Retrogression itself stays proven separately via the existing hand-verified `test_immigration_rules.py` fixtures (§7a) — this pass only wires the poll/loader layer.
- `agent/app.py`: new `POST /api/bulletin-poll` (body `{"month": "2025-09"|"2025-10"}`), and `ui/index.html` gets two buttons simulating the unattended poll firing for each month.
- Verified: `.venv/bin/python -m pytest -q` → 96 passed, 3 deselected (live). Smoke-tested via `uvicorn` + `curl`: September → silent/`not_current`; October → surfaced/`current` with a real attorney-review draft; repeat October → silent, same draft (dedup); missing month → visible `error`, no crash. Not browser-verified (Chrome extension still not connected).
- Next: C4 (recall through the shared engine, replacing the direct `recall_demo.json` read in `agent/app.py::recalls_check`).

**C2 done (2026-09-10) — persistent case/draft lifecycle:**
- `agent/engine/store.py`: new `drafts` table + `Store.save_draft()`/`get_draft()`, same sticky-upsert pattern as F3's `save_alert` fix — a `"ready"` draft is never overwritten (so it survives restart/reload untouched); a `"failed"` draft is not sticky, so a retry that succeeds replaces it and a retry that fails again is a safe no-op, not a lost action.
- `agent/packs/immigration/pipeline.py::run_discrepancy_check`: on a newly surfaced alert, builds and persists the draft. On a `silent` outcome (repeat poll, restart, or replay) it now looks up any existing `"ready"` draft and returns it instead of `None` — a reload/restart no longer loses access to an unresolved draft, while still producing no new ping (`decision` stays `silent`).
- `ui/index.html`: the result card now shows the draft whenever one exists (new or retrieved), not only when `decision === "surfaced"`.
- Existing tests updated to encode the new contract (`test_reprocessing_same_event_does_not_produce_a_second_draft` → `..._keeps_the_draft`); added restart-survival tests at the store, pipeline, and app layers.
- Verified: `.venv/bin/python -m pytest -q` → 82 passed, 3 deselected (live). Not yet browser-verified (Chrome extension still not connected in this environment).
- Scope note: did not add an approval/resolve UI step — the handoff explicitly marks that optional ("if approval is added..."); out of scope for this pass.
- Next: C3 (unattended bulletin poll), then C4 (recall through the shared engine).

**C1 done (2026-09-10) — real sample-document extraction wired end to end:**
- Three synthetic specimen-format images generated (`fixtures/sample_case/generate_specimens.py` → `fixtures/sample_case/specimens/{i94,i797,passport}.png`), coherent invented identity/dates across all three (matches the existing 55-day discrepancy beat).
- `agent/llm/document_client.py` (new): picks a live `bedrock-runtime` client when AWS credentials are configured, else `RecordedResponseClient` replaying `fixtures/sample_case/recorded_extraction_response.json` — either way the real `extract_fields()` validation logic (F2/F4 fixes included) runs unchanged; only the Converse response source differs. Every result carries an explicit `mode: "live"|"recorded"` label.
- `agent/packs/immigration/pipeline.py`: added `extract_sample_case_fields()` (runs extraction against all 3 specimens) and `run_sample_case()` (extraction → discrepancy rule → gate → draft; blocks the rule and returns `error` instead of running date math on an unreviewed/missing field).
- `agent/app.py`: `/api/sample-case` and `/api/process-sample-case` now run this real pipeline instead of reading a pre-extracted JSON fixture directly; both responses include `mode`. The stale `fixtures/sample_case/fields.json` was removed (superseded by the specimens + recorded response).
- `ui/index.html`: parse step shows the live/recorded mode and per-field status (missing/ambiguous), and the result card handles the new `error` (extraction-blocked) case.
- New live integration test (`tests/test_live_extraction.py`, `@pytest.mark.live`, excluded from the default run via `pyproject.toml`'s `-m "not live"`) — reads the real specimen bytes through live Bedrock when credentials exist; skips cleanly otherwise. Not yet run against real AWS in this session (no credentials in this sandbox).
- Verified: `.venv/bin/python -m pytest -q` → 74 passed, 3 deselected (live). Also smoke-tested via `uvicorn` + `curl` — both endpoints return correct fields/evidence/mode and a correct 55-day-gap draft. Could **not** visually verify in an actual browser (Chrome extension not connected in this environment) — JS syntax-checked with `node --check` and the F5 HTML-injection regression tests still pass, but a real browser check is still outstanding.
- Next: C2 (persistent case/draft lifecycle), then C3 (bulletin poll), then C4 (recall through shared engine) — in progress this session.

**F1–F5 fixed (2026-09-10), TDD throughout (regression test written and confirmed RED before each fix):**
- F1: `check_bulletin_cutoff` now requires priority date strictly before the cutoff (`<`, not `<=`); equality is `not_current`.
- F2: `extract_fields` now requires and sends `modelId` to Bedrock Converse. Added a botocore-schema-validating fake client (`tests/test_extraction.py`) so a missing required request param fails the suite, not just live calls.
- F3: `Store.save_alert` is now an atomic guarded UPSERT: a persisted `silent` row can transition to `surfaced` (previously stuck forever because the second INSERT silently failed on the PK conflict); a `surfaced` row is sticky and never downgraded; true no-op duplicates still return `False`. Also fixed a related bug this exposed — failed INSERTs weren't rolled back, leaving an open transaction that deadlocked a second connection on process restart. `_already_surfaced` (decision_gate.py) now matches on `rule_version` too, so a rule-version bump isn't silently suppressed by an older version's surfaced alert.
- F4: `extract_fields` now validates response shape end-to-end — malformed envelopes, non-dict JSON, and null field entries no longer crash; unsupported status values and "extracted" fields with a null value or empty evidence are downgraded to `ambiguous` and force `needs_review` (previously could return `needs_review=False` on a null value).
- F5: `ui/index.html` no longer interpolates any API/model text into `innerHTML`; all dynamic values render via `textContent`/DOM node construction, closing the HTML-injection gap.
- Full suite: 63 passed (up from 52 — 11 new regression tests), `.venv/bin/python -m pytest -q`. No AWS calls made; live Bedrock/AgentCore still unverified.
- Not yet started: C1–C4 (real sample-document extraction wiring, persistent case/draft lifecycle, unattended bulletin poll, recall through the shared engine) — see [the implementation handoff](docs/implementation-handoff-2026-09-10.md) for acceptance criteria on each, next in order.
- This session also made the **first commit of the entire implementation** (previously all uncommitted on `main` since the prior session) on branch `implementation/day2-bug-fixes`, opened as a PR against `main`.

**Prior handoff note (2026-09-10 AM), superseded by the above:** Read [the implementation summary and repair handoff](docs/implementation-handoff-2026-09-10.md) before continuing the build. It contains the reviewed baseline, confirmed bugs, missing demo paths, acceptance criteria, and a prompt for the next Claude Code session. Older entries below include superseded scope/completion claims.

**Repo:** https://github.com/anikeitvadi/agents-for-humans (private for now; must be flipped to public with an MIT LICENSE file before submission). Partner (DarshanHari19) accepted the collaborator invite (write access) by 2026-09-11.

**State:** idea chosen (2026-09-09): immigration status guardian, Everyday track. Two builders (Ani + partner). The ranked board is `docs/draft-board.html`; the idea trail is `docs/ideas-discussed.md`; the brief and rules are `docs/brief.md`; raw research is `research/`; architecture is `docs/architecture-spec.md`.

**Chosen build:** immigration status guardian is the flagship (document parsing → status math → attorney-email draft; unattended visa-bulletin beat off a captured bulletin). Recall-to-remedy is demoted to a thin second pack — one feed, one seeded receipt, one match, one ping — proving the engine is generic, not a co-headliner. Runner-up if this falls through: DataQs violation challenge (Professional).

**Architecture decisions locked (2026-09-09), reviewed against `docs/architecture-spec.md`:**
- Build order: plain Strands agent + domain logic (parsing, rules, decision gate) running locally first, no AWS dependency. AgentCore Runtime deploy is Day 3 work, only attempted if Days 1–2 are done.
- AgentCore Memory / Identity (Cognito) / Policy (Cedar) / Observability (OTEL) are **not build targets** — one "production path" paragraph + diagram in the README instead. Only Runtime + basic tool calling is in scope for the actual build.
- Day 1, hour 1: verify current AgentCore Runtime docs (what deployment actually requires) before scaffolding anything — the spec's Gateway/Policy/Memory details may reflect remembered docs, not current ones.
- Immigration date math (I-94 vs. passport expiry, 180-day bar) must be robust and correct — the pitch's credibility rests on it. Recall matching is explicitly demo-scope (pre-staged recall + receipt), labeled as such in the README so a judge testing their own receipt doesn't hit a fake match.

**Decisions waiting on the humans:** who owns what (backend/agents vs. feeds/UI vs. video — spec proposes Person A = engine/AgentCore, Person B = domain packs/dashboard/demo); repo name/rename from placeholder "Clockwork"; whether to add the moneylines decision engine (calibration loop) as a component (leaning no — out of scope for this build).

**Hard dates:** credits by Fri Sep 11 12pm PT (per person). Submit by Mon Sep 14 noon (closes 5pm PT). Ani is unavailable Thu Sep 10 around 2:00pm CT: nothing is scheduled against Wed night or Thu early afternoon.

**Timeline once chosen:** Wed: scope, repo skeleton, seed data, diagram. Thu after 2:30: agents + tools. Fri/Sat: integrations + the demo moment end to end. Sun: AgentCore deploy, live link, README, three builder.aws posts. Mon AM: video, submit.

**Rules to keep in mind while building:** new code only, disclose anything reused (jobsearch-cli logic, moneylines scripts) in the README; MIT license file at the repo root; architecture diagram; 5-minute video with problem / who / why.

**Plan review (Codex, 2026-09-09; recommendations, not accepted scope changes):** Read the implementation plan against the architecture, brief, idea trail, board, and immigration research. No implementation started. Revise before building: choose one coherent sample case and include the passport fixture; capture USCIS's monthly chart selection alongside DOS bulletins; remove automatic 180-day-bar conclusions; enforce rules/gate checks in application code; add extraction evidence/unknown handling, persistent event deduplication, and a fixed demo date. Move a minimal UI into the Day-2 end-to-end milestone; defer USCIS/Federal Register integrations unless needed for a demonstrated outcome. Bedrock remains an AWS dependency even with local hosting. Official rules confirm credits by September 11 noon PT (Friday in 2026), submission closes September 14 at 5pm PT. Next: resolve these plan corrections, then scaffold the agreed vertical slice. Existing ownership/naming decisions remain open.

**Updated-plan review (Codex, 2026-09-09):** Revised plan addresses the major earlier findings and is suitable to proceed after small contract clarifications. Remaining: make Strands Agent invocation part of the actual extraction/drafting path (decorating directly called Python functions is insufficient); have any Runtime entrypoint invoke the same deterministic engine; explicitly seed bulletin priority date/category/chargeability and source provenance; select chronologically coherent document fixtures and distinguish date-gap from days-remaining; show drafts before approval; enforce deduplication with a SQLite uniqueness constraint and distinguish pending/failed/ready drafting states. Separate offline tests from live Bedrock extraction checks. Existing architecture/spec text still describes superseded scope; reconcile before implementation. No code started or implementation authorized by this review request.

**Implementation started (2026-09-10), by Claude, TDD throughout — every module below was written test-first (RED confirmed, then GREEN):**

- **Scope shipped:** the corrected flagship only — an I-94/I-797 **document-date discrepancy check** (pure arithmetic, e.g. "these two dates disagree by 55 days"), never a lawful-status, F-1/OPT, or unlawful-presence-bar computation. That domain logic remains explicitly deferred to a later iteration, per Ani's direction ("these can be added once we build this initial implementation and test it").
- **Repo state:** `pyproject.toml`, `LICENSE` (MIT, not yet flipped public), `agent/` (engine + packs + llm + app.py), `ui/index.html` (static, `draft-board.html` design language), `fixtures/`, `deploy/agentcore_entrypoint.py` (untested — no AWS/AgentCore access in this environment), `tests/` — **52 tests, all passing**, no AWS credentials required to run them.
- **Deterministic boundary enforced in code, not a prompt:** `agent/engine/engine.py::process_event` is plain Python (validate → evaluate → gate → persist); addresses the earlier Codex review's point that decorating directly-called functions isn't sufficient enforcement.
- **Genuine Strands SDK usage** (addresses the "Strands invocation must be part of the actual path" review point): `agent/llm/draft.py::build_attorney_draft` invokes a real `strands.Agent` for the personalized intro only — the safety-critical boundary text is a deterministic core the LLM cannot edit, and any Agent failure/absence falls back to that core. `build_bedrock_agent()` returns `None` when no AWS credentials are configured (this sandbox), so tests stay fast and don't hang on network calls; `app.py` wires it in for real when credentials exist. Extraction (`agent/llm/extract.py`) intentionally stays on the raw Bedrock Converse API (multimodal document blocks) — verified as the AWS-recommended path, not routed through Strands.
- **Other review points addressed:** SQLite uniqueness constraint on `(clock_id, event_id, rule_version)` gives replay-safe dedup across restarts (tested); extraction returns per-field evidence + explicit `missing`/`ambiguous` status, never a confident guess; bulletin-cutoff logic requires the USCIS-designated chart to match the DOS chart provided, else `needs_review`; sample-case fixture uses I-94+I-797+passport (not I-20), chronologically coherent, gap is date-to-date not days-remaining-from-today (so no frozen "as-of" date was needed — removed dead `DEMO_AS_OF` config).
- **Not yet done:** `fixtures/bulletins/sample_month.json` is a clearly-labeled placeholder — needs real captured DOS bulletin + USCIS chart-selection page data before the demo. No explicit "approve before send" UI step (draft is shown, not gated behind an approval click). AgentCore Runtime deploy untested (Day-3 item, needs real AWS/AgentCore CLI access). Repo not yet flipped public; architecture diagram not yet exported as an image.
- **Docs reconciled:** `docs/architecture-spec.md` §4.2 and §7/§7a rewritten to match the corrected scope and the actual module layout (no more superseded-scope text).

**Implementation review (Codex, 2026-09-10; no implementation fixes made):** Reviewed the uncommitted working tree against HEAD `602e5ae` and the revised plan. Independently ran `.venv/bin/python -m pytest -q`: 52 passed, one deprecation warning. Confirmed blockers missed by tests: bulletin comparison uses `<=` although DOS requires an earlier priority date; Converse extraction omits required `modelId` (confirmed against installed botocore request schema); extracted null/empty-evidence fields can pass with `needs_review=False`; a silent event later becoming actionable returns `surfaced` repeatedly because its persisted silent row cannot be replaced and insertion failure is ignored. Malformed response shapes also escape extraction error handling. Drafts are transient, and the UI interpolates model output as HTML. Plan coverage remains incomplete: sample UI displays pre-extracted JSON with no specimen document assets or extraction pipeline; bulletin polling/pipeline is absent, beyond merely missing real source captures; recall endpoint reads a canned item and bypasses the shared gate/store. Next: fix verified correctness failures and wire the actual sample-document path before claiming the Day-2 milestone; then complete bulletin and recall integration. Live Bedrock and AgentCore were not exercised in this review.

**UI feedback (2026-09-10; recommendations only):** User wants a slicker, modern, serious/professional interface retaining dark undertones. Reviewed current screenshots and static UI. Recommend an action-first case workspace: compact header, prominent discrepancy/date comparison, supporting document evidence, collapsible attorney draft, and a quieter activity timeline; place recall in a separate view and demo/replay controls in a labeled utility area. Replace internal gate labels with user-facing outcomes and keep recorded/live provenance visible without AWS configuration text dominating the page. Motion for vanilla JavaScript plus Lucide icons fits the current stack; use restrained, state-driven transitions and reduced-motion support. No UI implementation or dependency installation performed; design recommendations await selection.
