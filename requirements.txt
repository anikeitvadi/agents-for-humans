# AgentCore Runtime deploy manifest (direct code deploy). The starter toolkit
# resolves dependencies from the source root and prefers requirements.txt over
# pyproject.toml, and pyproject's optional [deploy] extra is not installed
# otherwise. Keep in sync with pyproject.toml.
#
# Local development does NOT use this file: pip install -e ".[dev]"
strands-agents
strands-agents-tools
boto3
pydantic>=2
httpx
bedrock-agentcore
aws-opentelemetry-distro>=0.10.1
