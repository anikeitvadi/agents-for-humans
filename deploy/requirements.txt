# Runtime dependencies for deploy/agentcore_entrypoint.py (AgentCore direct
# code deploy). Mirrors pyproject.toml's runtime deps minus the local web
# server, plus the [deploy] extras.
strands-agents
strands-agents-tools
boto3
pydantic>=2
httpx
bedrock-agentcore
aws-opentelemetry-distro>=0.10.1
